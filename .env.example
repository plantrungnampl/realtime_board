DATABASE_URL=postgres://user:password@localhost:5432/dbname
JWT_SECRET=change_me_to_a_secure_random_string

# Database Configuration for Docker Compose
POSTGRES_USER=admin
POSTGRES_PASSWORD=change_me
POSTGRES_DB=realtime_board

# Internal Database URL for the backend container (uses service name 'db')
# This should match the POSTGRES_ variables above
DATABASE_URL_DOCKER=postgres://admin:change_me@db:5432/realtime_board

# pgAdmin Configuration
PGADMIN_DEFAULT_EMAIL=admin@admin.com
PGADMIN_DEFAULT_PASSWORD=admin

# Optional Redis Configuration
REDIS_URL=redis://localhost:6379
# Optional Email Configuration
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=user
SMTP_PASSWORD=password
SMTP_FROM=noreply@example.com
FRONTEND_URL=http://localhost:5173
