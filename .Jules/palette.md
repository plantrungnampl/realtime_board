## 2024-05-22 - Presence List Accessibility
**Learning:** Purely visual status indicators (colored dots) exclude screen reader users. Mapping status codes to human-readable labels in `aria-label` provides equal access.
**Action:** Always pair visual status indicators with descriptive text or ARIA labels and hide the visual element from AT.
